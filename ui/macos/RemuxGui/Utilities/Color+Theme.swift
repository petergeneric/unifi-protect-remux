import SwiftUI

extension Color {
    // MARK: - Status
    static let statusPending = Color.gray
    static let statusProcessing = Color.orange
    static let statusCompleted = Color.green
    static let statusFailed = Color.red

    // MARK: - Log levels
    static let logError = Color.red
    static let logWarn = Color.orange
    static let logInfo = Color.secondary
}
